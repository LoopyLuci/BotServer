package com.botserver.mobile.ui.theme

import androidx.compose.ui.graphics.Color

// Same palette as the desktop dashboard (desktop-app/ui/index.html) so the
// phone app and the desktop control panel read as one product, not two.
// Deliberately NOT using dynamicDarkColorScheme/dynamicLightColorScheme —
// Material You would recolor everything to the device wallpaper, which is
// the opposite of "look like Telegram."
val TgAccent = Color(0xFF3390EC)
val TgAccent2 = Color(0xFF2478D4)
val TgAccentSoft = Color(0xFFE3F1FF)
val TgAccentInk = Color(0xFF1467BF)
val TgLightBg = Color(0xFFE6EBEE)
val TgLightSurface = Color(0xFFFFFFFF)
val TgLightSurfaceVariant = Color(0xFFF4F6F8)
val TgLightInk = Color(0xFF1C2733)
val TgLightOutline = Color(0xFFE2E7EB)
val TgError = Color(0xFFE0473E)

val TgAccentDark = Color(0xFF4EA4F5)
val TgAccentDark2 = Color(0xFF2F8CE0)
val TgAccentSoftDark = Color(0xFF20344A)
val TgAccentInkDark = Color(0xFFBFE0FF)
val TgDarkBg = Color(0xFF0E1621)
val TgDarkSurface = Color(0xFF17212B)
val TgDarkSurfaceVariant = Color(0xFF1E2C3A)
val TgDarkInk = Color(0xFFE9EDF1)
val TgDarkOutline = Color(0xFF0C141D)
